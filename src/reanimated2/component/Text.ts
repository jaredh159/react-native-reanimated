import { Text } from 'react-native';
import createAnimatedComponent from '../../createAnimatedComponent';

const AnimatedText: any = createAnimatedComponent(Text as any);

export default AnimatedText;
